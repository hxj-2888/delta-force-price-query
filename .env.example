# 三角洲行动 - 变卖物价格查询
# 部署前必须设置 API_TOKEN 环境变量

# 本地开发 (server.js)
API_TOKEN=your_api_token_here

# Vercel: 在 Dashboard → Project Settings → Environment Variables 设置
# Cloudflare: 在 Dashboard → Workers & Pages → 项目 → Settings → Variables 设置
